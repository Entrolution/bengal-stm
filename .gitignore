target
.DS_Store
.claude/

# Quality-process working files — local logs, never part of the repository
/QUALITY.md
/.quality-accepted.jsonl
/*/*.iml
/.idea
/.idea_modules
/.project
/.settings
/.bsp
/src/main/resources/dataDecrypted/
scoverage-data/
scoverage-report/

# TLA+ tooling (specs/) — jar downloaded in CI, TLC run artifacts never committed
specs/tla2tools.jar
specs/**/*_TTrace_*
specs/**/states/

# TLC drops states/ in the working directory
/states/
